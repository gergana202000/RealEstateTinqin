package com.tinqin.estate.core.processor;

import com.tinqin.estate.api.base.Error;
import com.tinqin.estate.api.model.contract.ContractRequest;
import com.tinqin.estate.api.model.contract.ContractResponse;
import com.tinqin.estate.api.operation.SoldEstateByCityProcessor;
import com.tinqin.estate.domain.data.repository.EstateRepo;
import io.vavr.control.Either;
import io.vavr.control.Try;
import org.springframework.core.convert.ConversionService;

public class SoldEstateByCityProcessorCore {/*implements SoldEstateByCityProcessor {
    private final ConversionService conversionService;
    private final EstateRepo estateRepo;

    public SoldEstateByCityProcessorCore(ConversionService conversionService, EstateRepo estateRepo) {
        this.conversionService = conversionService;
        this.estateRepo = estateRepo;
    }

    @Override
    public Either<Error, ContractResponse> process(ContractRequest input) {
        return Try.of(()->{

        });
    }*/
}
